# Project Claude Instructions

## File System Operations
For any file system operations (listing directories, reading files, searching, etc.), always consider using the `mcp__filesystem__*` tools as an additional option alongside or instead of PowerShell/Bash commands.

